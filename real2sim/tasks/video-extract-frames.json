{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "krabby:task/video-extract-frames/v1",
  "title": "video-extract-frames",
  "description": "Lossless full-frame extraction video -> input/src/ (Recipe A steps 1-2). Never fps-subsample at extraction; selection is a later task's job.",
  "x-task": {
    "kind": "preprocessing",
    "phase": 1,
    "operator": false,
    "image": null,
    "entrypoint": "ffmpeg -fps_mode passthrough (canonical one-liner, RECIPES.md Recipe A)",
    "code_ref": "real2sim/RECIPES.md#1-video-frame-pool",
    "story": "STO-SCN-054",
    "inputs": [
      {
        "name": "video",
        "pattern": "input/*.{mp4,mkv,mov}"
      }
    ],
    "outputs": [
      {
        "name": "frame_pool",
        "pattern": "input/src/frame_*.png"
      }
    ],
    "produces_variables": [
      "n_frames"
    ]
  },
  "type": "object",
  "properties": {
    "format": {
      "type": "string",
      "enum": [
        "png"
      ],
      "default": "png",
      "description": "lossless only; jpeg extraction would re-compress"
    }
  },
  "required": [],
  "additionalProperties": false
}
