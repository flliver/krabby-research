---
assigned: 2026-06-16
source: project:baeprz
priority: normal
re: EPI-SCN-FLEET-IMAGE-DEPLOY (STO-SCN-156, STO-SCN-158)
---

# ✅ ops complete — STO-SCN-156 + STO-SCN-158

Both operator-greenlit items done. **You own closing the Beads artifacts** (they live in krabby's beads, not baeprz's).

## STO-SCN-156 — canonical fallbacks pushed to registry as :0.1
| repo:tag | digest |
|---|---|
| `krabby-mast3r:0.1` | `sha256:481571cbbb6e8e…` (b's Apr-29 canonical) |
| `krabby-mast3r-base:0.1` | `sha256:d57049231d13…` (s) |
| `krabby-slam3r:0.1` | `sha256:d95c509577ba…` (d) |
| `krabby-vggt:0.1` | `sha256:6adb513a80f3…` (d) |

Registry catalog: `krabby-{da3,fastmap,mast3r,mast3r-base,matcha,slam3r,vggt}`. `011-scene-reconstruction(-cuda)` NOT pushed — tar-only per STO-SCN-160.

## STO-SCN-158 — fan-out playbook
`fleet/ansible/docker-pull.yml`, committed **40d5d82** (baeprz trunk).
- Reads your `fleet-manifest.yaml` as source of truth at runtime (single source; override `-e manifest_path=`).
- `gaming_nodes:!j`; WoL via inventory `wol_mac` (t/s2idle = manual-wake-only, flagged).
- parallel `docker pull` active_path; `-e pull_fallback=true` adds fallback[]; `-e do_pull=false` = matrix-only.
- additive-only (no prune/retag path exists).
- driver-independent RepoDigest sync matrix (overlay2 t vs containerd b/d/s).
- **Verified GREEN**: dry run over already-synced active path = clean no-op (changed=0); all 4 hosts IN-SYNC on matcha 0.2.2-selfcontained / da3 0.4 / fastmap 0.3.

## Open (low-pri)
`:latest` finalization — digest-check t's `011-cuda` + `fastmap 0.1/0.2` vs s/d to confirm truly-unique preserved copies. Say the word.

Full preservation set intact; nothing pruned/retagged anywhere.

## Status Notes
- 2026-06-16: Completed by baeprz-ops. Deliverables also in `/var/krabby/inbox/EPI-SCN-FLEET-IMAGE-DEPLOY/`.

- 2026-06-16: Actioned by liaison — STO-SCN-156 + STO-SCN-158 both flipped to shipped/closed (DoD ticked, status notes recorded). Epic stays in_progress (open children: 150-153,155,160).
