<!-- paths-note -->
> **Paths note (2026-09-09):** this campaign moved from `sim_fine_tuning/2026-08-26_0200_teacher_handoff/` to `parkour/parkour_tasks/parkour_tasks/crab_hex_forward_task/experiments/2026-08-26_0200_teacher_handoff/`. Absolute paths below (`/home/.../sim_fine_tuning/...`, `parkour/logs/rsl_rl/...`) name raw artifacts that stay on disk untracked; the tracked checkpoint of record is `head/` (see `bundle.yaml`) and the eval summaries are in place / under `evals/`.

# Teacher-stack hand-off (M1C -> bridge -> 2b1 -> 2b2), new plant — CHANGELOG

Base: the baked reference M1C `2026-08-25_19-09-15/model_24900.pt` (flat 0.604/0.94,
obstacles 0.563/0.57). First teacher hand-off on the measured-hardware plant.
Command band rescaled to the plant envelope via the new `_teacher_lin_vel_x_band()`
override: KRABBY_LIN_VEL_X=0.20:0.40 (old hardcoded 0.45-0.85 belongs to the 6 rad/s-cam
plant). Clock obs advance in all stages (base_velocity present); clock/RSI/apex REWARDS
are flat-stage-only by design — stage stacks take over, the gait lives in the net.

| stage | budget | train signals | gate | verdict |
|---|---|---|---|---|
| bridge→2b2 chain #1 | 2k/2k/3k ungated | bridge failure 0.74 (old plant 0.18), 2b1 0.96, 2b2 0.99 (ep len 92) | — | **COLLAPSED — ungated promotion compounded a struggling bridge** (my design error: the old campaign's batch-gate protocol exists for exactly this). Diagnosis: no spawn/DR issue (pushes off in bridge-lite; early stats were warm-up artifacts); bridge genuinely at 60-80% failure = adaptation shock (goal-heading obs, 0.40 band top, new reward stack) needing scale. Chain #2: GATED — 2k chunks, promote only at failure < 0.35, cap 8k per stage |
| chain #2 | gated 2k chunks | bridge: failure 0.85→0.98→1.0→1.0 (MONOTONE DESTRUCTION, not adaptation) | GATE FAIL, chain stopped at bridge | **Root cause found: bridge economics net ≈ −3.2/step on this plant** — positive income tuned for old-plant speeds (tracking earns 0.23; goal terms 0) while the torque tax (−2.88) and low-speed penalty (−3.0 weight, min_actual_speed 0.35 > this plant's steady 0.10-0.20) keep charging → dying early is optimal. Fix: clock schedule income registered in the teacher bridge family (KRABBY_CLOCK_W, inherited by 2b1/2b2) + KRABBY_MIN_ACTUAL_SPEED override (0.08). Chain #3 launched with repaired economics |
| chain #3 | economics repaired (clock income live 0.12-0.16, low-speed defanged −0.013) | bridge failure **0.00 at start** → 0.64@400 → 0.90@2000 — pure TRAINING-INDUCED DESTRUCTION (M1C walks fine in the teacher env untouched) | GATE FAIL | **Mechanism: stage-boundary critic mismatch** (plasticity review §3.1) — flat critic prices states at ~0.5/step income, bridge nets ~0.0015 → advantages hugely negative everywhere → PPO unlearns the gait. Fix: critic reset + optimizer clear on M1C (norm 84.5→17.3) via the existing B1 tool. Chain #4 from the reset checkpoint |
| chain #4 | critic reset at hand-off | **bridge GATED in 1 chunk: failure 0.25** (old plant 0.18) — critic-mismatch diagnosis CONFIRMED | bridge PASS; 2b1 then degraded 0.58→0.91 (same destruction curve at the NEXT boundary) | The value-scale cliff recurs at EVERY stage boundary (2b1 adds goal income). Chain stopped mid-2b1 to save 2 doomed chunks. Chain #5: critic reset at every boundary; starts at 2b1 from the reset bridge winner (2026-08-26_12-38-32/model_26899_critic_reset.pt) |
| chain #5 | 2b1 from reset bridge winner | failure 0.96-0.99 all 4 chunks; **early curve 0.00→0.33@400→0.96@2000 tracking the goal income upward** | GATE FAIL at 2b1 | Critic reset irrelevant here — different mechanism: **capability gap.** The formation campaign froze heading; the policy has never turned. Full-dose goal/yaw income (goal_vel 0.75, yaw 0.2) pays for maneuver attempts the gait cannot survive — the better it tries, the more it dies. Fix: Freitag dose ramp (KRABBY_GOAL_VEL_W/KRABBY_YAW_W plumbed): chain #6 = 2b1 at 25% dose (gated) → 100% (gated) → critic reset → 2b2 |
| chain #6 | 2b1 dose ramp (25%→100%) | STOPPED BY USER mid-chunk-1 of the quarter-dose leg | — | Hand-off paused. State: bridge BANKED (2026-08-26_12-38-32/model_26899, failure 0.25; critic-reset variant alongside); 2b1 unsolved (full dose = lethal capability gap, quarter dose untested); 2b2 untouched. Pre-declared fallback if the ramp is abandoned: flat turning school on M1C before re-entering the chain |
